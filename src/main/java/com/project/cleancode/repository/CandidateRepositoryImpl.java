package com.project.cleancode.repository;

import com.project.cleancode.domain.Candidate;

public class CandidateRepositoryImpl implements CandidateRepository {

    @Override
    public Integer save(Candidate candidate) {
        return 1;
    }
}
